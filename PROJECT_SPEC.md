# Spectral Calibration Lab 项目规格

## 项目目标

提供一个可离线运行的 Go HTTP 服务，帮助实验室对光谱采样批次进行校准轮次管理、质量评分与结果回顾。系统不依赖在线数据库，所有状态仅保存在进程内存中，便于重复运行验证。

## 用户角色

- 实验室操作员：选择校准方案并提交采样读数。
- 研究工程师：查看读数的中心偏差、离散度与质量结论。
- 方法维护人：维护各校准方案的参考中心和容忍范围。

## 核心实体

- 校准方案 Profile：包含参考中心、容忍范围、最少采样数与解释标签。
- 观测批次 Observation：一组同一方案下的原始数值。
- 校准轮次 Run：观测被解析、评分后的结果。
- 质量摘要 QualitySummary：方案近期轮次的聚合结论。

## 业务流程

### 查看校准方案

操作员访问 `GET /v1/profiles`，服务返回可选方案、参考中心和容忍范围。成功时返回方案数组；没有可用方案时返回空数组。

### 提交观测批次

操作员调用 `POST /v1/observations` 并提交方案标识与至少三个数值。服务校验输入、计算中心偏差和离散度，创建校准轮次。成功时返回 `accepted` 状态；不存在方案或读数不合法时返回可识别错误。

### 查询质量摘要

研究工程师调用 `GET /v1/reports?profile_id=<id>`。服务结合方案和已保存轮次计算总数、最近评分与建议动作。成功时返回质量摘要；未知方案返回不存在错误。

## 状态与规则

轮次的状态在 `accepted`、`review`、`repeat` 三者之间确定。读数数量必须不低于方案要求；中心偏差和离散度共同决定评分。评分低于方案阈值时，服务建议重新采样；评分适中时建议人工复核；高分轮次可以接受。

## 接口与验证

服务提供 `/healthz`、`/v1/profiles`、`/v1/observations` 与 `/v1/reports`。运行清单会实际启动服务，检查健康接口，读取方案，提交观测，并读取指定方案摘要。
